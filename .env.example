# Copy to .env and fill in real values. Never commit .env.
TELEGRAM_BOT_TOKEN=
# Optional: Gemini (guest chat + chalkboard OCR). Leave unset to use local Ollama.
# GEMINI_API_KEY=
# GEMINI_CHAT_MODEL=gemini-flash-latest
# GEMINI_BOARD_MODEL=gemini-flash-latest
# PORT=3000
